SELECT FirstName, LastName, City, State FROM Person LEFT JOIN Address on Person.PersonID = Address.PersonID;

//use an outter join here (left join) since there might not be an address for every person
